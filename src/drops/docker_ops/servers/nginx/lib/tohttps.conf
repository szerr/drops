
server {
    listen 80;

	# acme.sh 证书文件验证路径
	location /.well-known{
		root /var/www;
	}

	location / {
		if ($host !~* '^www.*'){
			return 301 https://www.$host$request_uri;
		}
    	return 301 https://$host$request_uri;
	}
}
